interface:
  display_name: "Trigger Profile README Workflow"
  short_description: "Trigger and verify profile README workflow"
  default_prompt: "Use $trigger-profile-readme-workflow to trigger the VectorPeak profile README workflow and verify whether it succeeded, created a PR, or exited with no README diff."
